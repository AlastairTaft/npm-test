{
  "name": "npm-test",
  "version": "0.0.1",
  "description": "Test to repo the bug with the same git package at multiple levels",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
